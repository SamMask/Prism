# Prism V2.6 Pi Live Deployment Evidence — 2026-07-14

## Scope

- Target: `PI5Mask24` (`aarch64`), `/home/mask070924/prism`
- Live owner: `prism-go-primary.service` on `127.0.0.1:5004`
- Public entry: `https://prism.local` through Caddy
- Deployment command: `powershell -ExecutionPolicy Bypass -File scripts/go_primary_pi_live_ops.ps1 -Mode Cutover -DeploySnapshotKeep 5`
- Boundary: no schema/API/data-dir/public-exposure change; Pi user DB, uploads and attachments were not replaced by build artifacts.

## Preflight

- SSH alias and non-interactive sudo passed.
- Disk use: 6%; live DB existed and was approximately 4 MiB; deploy snapshot policy already retained five snapshots.
- Live version before cutover: `2.5`.
- Migration: current/latest `17`, pending `[]`.
- Consistency: `healthy`, foreign keys enabled, no orphan note-tag rows.
- Search integrity: 255 Notes rows and 255 FTS rows, no missing/orphan FTS rows.
- Caddy already routed to Go primary with `X-Prism-Go-Primary: hit`.
- Drift found before cutover: legacy `prism.service` was unexpectedly active/enabled alongside Go primary. It was retained through the cutover safety window, then disabled only after the Go full-workflow smoke passed.

## Cutover and Snapshot

- Deployed artifact SHA-256: `e650c5a0990d4ed36308d83a06c453b545cb50bb1da7a05b7c42986989685a36`.
- Pre-cutover snapshot: `/home/mask070924/prism/backups/go-primary-t042-20260714_010616`.
- Snapshot contains SQLite backup, `static/uploads` / `docs/attachments` data tar, Caddy backup and service-unit backups.
- Snapshot retention passed: newest 5 kept, oldest 1 removed, no retention errors.
- Local machine-readable evidence: `build/go-primary-live/pi/evidence.json`, `t042-cutover.json`, `t042-full-workflow.json`, `t042-post-smoke.json`, `t042-deploy-snapshot-retention.json`.

## Functional Evidence

- Canonical T042 full-workflow smoke passed over Caddy:
  - health
  - note create/update/search/delete
  - image upload/serve/reference cleanup
  - JSON export/import and cleanup
  - orphan image scan/delete
  - SQLite backup response
  - migration status
- Post-smoke DB and uploads/attachments tree hashes matched pre-cutover hashes.
- Final ownership:
  - `prism-go-primary.service`: active/enabled
  - `prism.service`: inactive/disabled
  - `caddy`: active
  - only `127.0.0.1:5004` remained listening for Prism runtime traffic
- API read-back:
  - `/healthz`: schema 17, public bind blocked, Go write/file/import/export/server surface enabled
  - `/api/server/version`: `2.6`
  - `/api/system/migration-status`: current/latest 17, pending empty
  - `/api/notes?q=prompt`: success
  - `/api/categories`: success
  - `/api/system/check-consistency`: healthy
  - `/api/system/search-integrity`: 255/255 aligned

## Bounded Soak and Browser Smoke

- Five independent read-soak samples passed after cutover.
- Every sample kept Go/Caddy active, legacy inactive, version 2.6, schema 17 and pending migrations empty.
- Go RSS range: 34,304–35,136 KiB.
- WAL size stayed stable at 4,243,632 bytes during the captured samples.
- Go primary and Caddy error-priority journal query since cutover returned no entries.
- Playwright opened `https://prism.local` with the Caddy internal certificate accepted for this local smoke.
- Home rendered live note/category/tag counts and the Search Workspace controls.
- Command Palette `? prompt` server search returned 52 results.
- Served JS contained the V2.6 workflow hooks for saved search, command-palette server search, batch-delete preview, import preview and ReadingView source URL panel.
- Browser console: 0 errors, 0 warnings.

## Finding

- Non-blocking version-label drift remains: the browser page title and sidebar brand show `Prism V2.5` because `frontend/index.html` and the current frontend brand copy still contain the old label.
- This is not a stale Pi artifact: the same label exists in current source, while `/api/server/version` correctly reports `2.6`.
- It was not patched during the live deploy because doing so would make the Pi runtime diverge from the already published V2.6 release artifact. Handle it in a separate small frontend/version-label gate with build, browser smoke and an explicit republish/redeploy decision.
