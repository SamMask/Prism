"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**ss

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
"Antigravity" (基於 Jetski/VS Code 核心) 的編輯器「交互式教學 (Interactive Playground)」。

它不是一般的說明書，而是一個實戰演練場，目的是讓你通過實際操作，快速掌握 9 個核心生產力功能。

以下是基於你提供的截圖，按功能模組進行的戰略拆解與翻譯：

1. 核心編輯效率 (Editing Efficiency)
這是提升 Coding 速度的基礎，重點在於「不使用滑鼠」完成複雜操作。

Multi-Cursor Editing (多游標編輯)：這是文件中的重點。允許你同時在多個位置輸入或刪除。

區塊選擇 (Box Selection)：按住 Shift + Alt + 方向鍵，可以像畫方塊一樣選取文字。

新增游標 (Add a cursor)：Ctrl + Alt + 上下鍵，或 Alt + 滑鼠點擊。

選取所有相同字串 (Select all occurrences)：選中一個詞（如 background-color），按 Ctrl + Shift + L，可一次性修改全文中所有該詞彙。

Line Actions (

---
📎 **[完整內容已分離為附件]**

> 此筆記內容過長，已自動分離為附件。點擊附件可查看完整內容。
